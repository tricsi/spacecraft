body, html, canvas {
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
}

body, html {
    overflow: hidden;
    background: #000;
    color: #0f0;
    font-size: 15px;
    font-weight: bold;
}

#hud {
    position: absolute;
    margin: 20px;
}